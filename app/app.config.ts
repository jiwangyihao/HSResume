export default defineAppConfig({
  ui: {
    colors: {
      // Map Nuxt UI's semantic `primary` color to Tailwind's `blue` palette.
      // This makes classes like `text-primary-600` resolve to `blue-600`.
      primary: "blue",
    },
  },
});
